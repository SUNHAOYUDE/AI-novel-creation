export interface LLMProvider {
  name: string;
}

export interface StorageProvider {
  name: string;
}

export interface CommentSourceProvider {
  name: string;
}

export interface WorkflowProvider {
  name: string;
}
